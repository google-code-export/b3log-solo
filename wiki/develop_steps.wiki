#summary 开发步骤

===初始框架===
 # 在 /skins 下新建一个文件夹${myskin}，命名可任意，系统会自动识别出新增的皮肤文件夹。如无特别声明，以下新建的文件或文件夹均位于${myskin}中
 # 新建 skin.properties 文件，在 skin.properties 中对 name 和 memo 赋值，其中：
    * name：皮肤名称（此名称将作为皮肤名列在<后台管理-偏好设定-皮肤>中）
    * memo：备注，可记录所仿皮肤的站点或名称等信息
 # 新建 default-index.css 文件，用于写入该皮肤的外联样式
 # 新建 emotions、images、js 文件夹（必要时）
    * emotions：存放该皮肤的评论头像图片，尺寸最佳为24×24，数量最佳为15，命名为 em00.png ~ em14.png
    * images：存放该皮肤所需要使用到的其他图片
    * js：存放 /js 中没有，但该皮肤需要使用到的 js 文件
 # 皮肤截图保存为 preview.png 作为皮肤预览
----
===具体开发===
 # 可参照 /classic 目录中的 ftl 文件进行创建及相应的修改，从文件名上基本可推断其作用
 # 建议从 index.ftl 开始修改，只要 index.ftl 的结构确定下来，除个别外，其他页面的结构与其基本类似甚至相同。当然，其中通过 <#include "xxx.ftl"> 包含了一些通用的页面
    * common-top.ftl 是博客最上面的一条，作为整个博客的管理通道。基本上可以不做改动
    * article-header.ftl 是博客的头部，包含了博客的标题，副标题，以及页面导航和统计数据等信息
    * common-articles.ftl 是博客的文章列表及分页显示部分，其中包含了文章的发布时间、文章标题、文章摘要、所属标签、浏览总数、评论总数等基本信息
    * article-side.ftl 是博客的侧边栏，罗列了公告、最新评论、评论最多和访问最多的文章、分类标签、友情链接以及存档等
    * article-footer.ftl 是博客的尾部，包含了一些 Copyright 等信息
 # page.ftl 是新建页面的模板，在博客后台的页面管理中新建的页面均以该模板的结构来显示
 # 除以上文件外的其他文件都是在点击某项目时进入的页面的模板
    * archive-date-articles.ftl 是在点击了存档列表中的某项后
    * article-detail.ftl 是点击了文章名后
    * error.ftl 是在发生 401 等错误后
    * tag-articles.ftl 是在点击分类标签列表的某项后
    * tags.ftl 是在点击了标签墙后
----
===注意事项===
 # 皮肤修改过程中，id 最好不要进行更改
 # 部分公用方法被提取到 /js/util.js 和 /js/articleUtil.js 
 # 如有任何疑问请与 [http://vanessa.b3log.org Vanessa] 联系